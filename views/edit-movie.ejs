<!DOCTYPE html>
<html lang="en">
<head>
    <title>Edit Movie</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body class="container mt-5">
    <h2>Edit Movie</h2>
    <form action="/admin/edit-movie/<%= movie._id %>" method="POST">
        <div class="mb-3">
            <label>Movie Name</label>
            <input type="text" name="name" class="form-control" value="<%= movie.name %>" required>
        </div>
        <div class="mb-3">
            <label>Description</label>
            <textarea name="description" class="form-control" required><%= movie.description %></textarea>
        </div>
        <div class="mb-3">
            <label>Tags (comma separated)</label>
            <input type="text" name="tags" class="form-control" value="<%= movie.tags.join(', ') %>" required>
        </div>
        <div class="mb-3">
            <label>Download Link</label>
            <input type="text" name="downloadLink" class="form-control" value="<%= movie.downloadLink %>" required>
        </div>
        <button type="submit" class="btn btn-success">Update Movie</button>
        <a href="/admin/dashboard" class="btn btn-secondary">Cancel</a>
    </form>
</body>
</html>
